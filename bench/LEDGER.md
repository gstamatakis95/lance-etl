# Benchmark ledger

This file did not exist before 2026-07-21. It is created here with its first entry. Append one
row per qualification or Mode A/B run worth comparing against, newest entries at the top. Record
enough inputs (dataset, scale, batch count, seed, focus) to make a later run comparable, plus the
headline outputs.

## Runs

| Date | Run ID | Dataset / scale | Batches | Focus | Key results | Notes |
|---|---|---|---|---|---|---|
| 2026-07-22 | fuzz-pr04-seed42, fuzz-pr04-seed7 | fuzz CRUD, 2 orgs, keyspace 120 | 5 snapshots each | **PR-04 (production-readiness review): fuzz oracle coverage extended to resurrection and double-delete.** `bench/fuzz_workload.py` previously never revived a tombstoned key or re-deleted an already-dead one, so the fuzz e2e being green said nothing about the `when_matched_update_all` watermark resurrection path or a second tombstone landing over an existing one in `etl/replay_sink.py`. Added a `dead: set[str]` to `WorkloadBuilder`, populated only by a normal live-key delete and never by an absent-key delete, since a key that was never alive cannot be resurrected. Added a `maybe_revive` branch inside `do_insert` (30% draw, resurrects a dead key with a bumped `payload_version`, scenario `revive`) and a `maybe_redelete` branch inside `do_delete` (25% draw, re-tombstones a dead key, scenario `redelete`). Both draw exclusively from `dead` and use the same recent `fresh_ts()` band as every other op, so retention mode (unused in these runs) would never expire them mid-run out of band. `maybe_duplicate` already excludes both by its existing `scenario == "normal"` gate, so no change was needed there. The oracle needed no code change to key on the new scenarios, since `replay_state` already replays in generation order and keys by `record_id`. `OracleRow` did gain a `scenario` field so `bench/fuzz.py` could add explicit `resurrection_evidence` (mirroring the existing `retention_bands` evidence pattern): one entry per key whose terminal op was a `revive`, cross-referenced against the actual published row's `is_deleted`. Non-default fuzz knobs (`--fuzz-ops 300 --fuzz-snapshots 5 --fuzz-keyspace 120 --fuzz-mix 45:20:35 --tenants 2`, dup/retention/dim/num-clusters left at default) were used for both runs to reliably accumulate a `dead` population early and keep exercising it across the remaining snapshots. An offline check of `generate_workload`/`oracle_rows` at these exact knobs across seeds 42, 7, 123, and 2024 confirmed every seed produces both scenarios and at least one terminally-live revived key before any Spark time was spent. | **Both seeds green end to end.** Seed 42: `op_scenarios` summed across snapshots `{normal: 250, absent_delete: 9, duplicate: 17, revive: 29, redelete: 14}`. `resurrection_evidence` non-empty on both orgs (org0: 10 entries, org1: 9), every entry `published_live: true`, and `mismatches: []` for both orgs — so every revived key is asserted live *with* its regenerated payload matching (vector/text/cluster), not merely present. `verification.ok == true`, `conflict.enabled == false` (not exercised this run), all 6 declared indexes present, `distinct_equals_total == true`. Seed 7: `op_scenarios` summed `{normal: 251, absent_delete: 15, duplicate: 21, redelete: 13, revive: 23}`. `resurrection_evidence` org0: 4 entries, org1: 7, every `published_live: true`, `mismatches: []` both orgs, `verification.ok == true`. No product bug surfaced. The replay sink's resurrection and re-delete paths behaved exactly as the tombstone-retention/anti-resurrection-watermark design in `src/lance_etl/AGENTS.md` describes. New unit tests bring `pytest -m "not integration"` to 826 passed, up from an 811 baseline that already included unrelated concurrent work in the tree, zero failures: `tests/test_bench_fuzz_workload.py` (13 tests covering `maybe_revive`/`maybe_redelete`/`do_delete`'s `dead`-population plus a real two-seed `generate_workload` statistical check at the exact real-run knobs), `tests/test_bench_fuzz.py` (5 tests for `resurrection_evidence`), and tests added in the same pass for PR-07/08/09 (7 in `tests/test_bench_reconcile.py` for `drain_reconciler` retry-quiescence, 6 in `tests/test_bench_search.py` for `search_headline_status`). `ruff format`/`check` clean on `src/ tests/ bench/ migrations/`. No stray `search-api`/Spark process or `lance_bench_e2e_*` schema left in `lance_etl_bench` after either run, verified via `pg_namespace` and `ps aux` before and after both runs. One early startup line appeared in both runs' logs: a background `tokio-rt-worker` thread panic, `unexpected error when polling the I/O driver: Bad file descriptor`. This is a benign, non-fatal warning from a Rust runtime worker unrelated to this change. Both runs completed to a full, correct `ok: true` verification afterward with no other error or traceback in either log. | Also closed in the same pass, all in `bench/` (separate findings from the same review, not re-described in this fuzz-focused row). **PR-07**: `bench/search.py` standalone `search` phase no longer hardcodes `status: "MEASURED"`. A new `search_headline_status(load, sweep)` derives `FAILED` from a fully-rejected load leg or a zero-query sweep, and the phase now raises (mapped to CLI exit 1) after still saving `search.json`. **PR-08**: `bench/reconcile.py:drain_reconciler` no longer declares quiescence on a zero-claim cycle alone. It now also consults `repository.control_plane_status()` and keeps cycling (brief `time.sleep`) while `retry_wait_work`/`due_work` are nonzero. **PR-09**: `bench/grpc_client.py:open_stub`/`open_ready_stub` now return the channel alongside the stub so `bench/search.py` and `bench/e2e.py` close it in a `finally`. `bench/search_server.py`'s log-file-open-plus-`Popen` and `bench/reconcile.py`'s `admin_engine` creation were each moved inside their own `try`/`finally` so an exception in the short window before the original `try` no longer leaks the resource. `bench/prepare.py`'s Spark session is now created immediately before its own `try`. |
| 2026-07-21 | 20260721T162043Z | sift1m 1M, WITH text | 2 | **4-angle code-quality pass over `bench/`**: 11 deduped fixes across `e2e.py`, `grpc_client.py`, `config.py`, `reconcile.py`, `corpus.py`, `search.py`, `cli.py`, `smoke_client.py`, and `src/lance_etl/AGENTS.md` (stale `bench/server.py` reference, dead `timed_call` metadata param, `search_api_binary` default declared twice, redundant `search_leg_attempted` re-derivation, a combined `open_ready_stub`/`generate_and_load_stubs` helper replacing the duplicated protoc→load→open trio at every call site, stub generation reordered before `self_hosted_search_api` starts the subprocess, `run_load_level` narrowed to catch only gRPC `RESOURCE_EXHAUSTED` and re-raise every other status, one traceback on CLI failure instead of two). The load-bearing fix for this run's proof is **fix 6**: `bench/reconcile.py:production_slice_batch`'s text-generation loop constructed a fresh `np.random.default_rng([seed, 2, cluster, index])` per row inside the `mapInArrow` executor closure. It was replaced with new `bench.corpus.batch_row_texts`, which seeds exactly one generator per ingest task (keyed by the batch's first global index) and vectorizes per-row without-replacement sampling with a random-key top-k trick, grouped by cluster so each row's terms still come only from its own cluster's vocabulary (the FTS hit-rate invariant). `row_text`/`text_for_row`/`prepare.py` were left untouched (per-row RNG-key compatibility with the old scheme was explicitly not required, only per-run determinism). | Reconcile drain: batch 0 — 500,000 rows, published at Lance v63, 66.053s. Batch 1 — 1,000,000 rows, published at v124, 38.7s. That is faster than this ledger's most recent comparable batch-1 number, 43.307s, consistent with removing per-row generator construction from ingest (batch-0 is within normal first-batch JVM/Ivy warmup noise). `publication_verification.ok == true`, all 6 declared indexes present, no `missing_indexes`. Zero commit retries in this successful run. `final_catalog_grpc.status == "MEASURED"`: cold_ms 151.631 / warm_ms 4.148. **recall@1 0.9838, recall@10 0.9885, recall@100 0.9101** (target band from the task was 0.9833/0.9885/0.9111, within normal ANN run-to-run noise since fix 6 touches text only, never vectors or ground truth). **FTS: hit_rate 1.0** (100 queries, mean 5.421ms) — structurally guaranteed by construction (each row's cluster terms are drawn only from its own cluster's pairwise-distinct vocabulary), so this is the expected, provably-correct value, not a coincidence. Hybrid (RRF) recall@10 0.498 (not a pass/fail criterion for this task, close to the prior 0.494, expected to differ since the exact sampled words changed). ruff format + check clean on `src/ tests/ bench/ migrations/`. pytest: 786 passed (`-m "not integration"`, 780 baseline plus 6 new tests added for `batch_row_texts` and the narrowed `run_load_level`/`timed_call` surfaces), zero failures. No stray `search-api` process or `lance_bench_e2e_*` schema left after the run, verified via `information_schema.schemata` on `lance_etl_bench` (`lance_etl` was never queried or touched). | **A first attempt at this run failed with `lance_etl.etl.completion.CompletionConflict: same window sequence carries a different source digest` on batch 1.** This was not a bug in fix 6, but the completion-marker correctness guard doing exactly its job: `bench/workspace/lance/d8521b06-*.lance` still held completion markers from an earlier run under the *old* per-row text algorithm, and fix 6's batch-vectorized algorithm deterministically produces different (but still fully seed-deterministic) text bytes for the same window, so replaying that window against the stale physical dataset correctly tripped the digest mismatch. Fix: deleted `bench/workspace/lance/` (safe, derived benchmark output, keyed by a deterministic dataset id independent of the control-plane schema) before re-running, the same remediation the `20260721T144407Z` row below used for its own text-algorithm change. No `prepare.py` change was needed since fix 6 explicitly did not touch the prepare path, and the existing `n1000000-t1-s42-c64` prepared artifacts (manifest confirmed `no_text: false`, matching shape) were reused as-is. |
| 2026-07-21 | 20260721T144407Z | sift1m 1M, WITH text | 2 | **FTS vocabulary-disjointness fix**: before this run, index-usage verification for run `20260721T140000Z` was independently confirmed with four evidence lines against the published v132 dataset — (1) code path: `rust/search-api/src/lance/backend.rs:run_vector_query`/`run_text_query` always call `scanner.nearest`/`scanner.full_text_search` with no silent flat-scan fallback (`bypass_vector_index` is hardcoded `false` in `grpc/convert.rs` and reserved out of the proto, so no client can ever set `use_index(false)`). (2) Runtime telemetry from that run's capture showed `search_api.cache.lookup` tag `index/memory/hit` 943,425 times against 10,002/100/99 `vector_search`/`text_search`/`hybrid_search` RPCs with 0 failed queries. (3) `lance.dataset(uri, version=132).scanner(nearest={...}).explain_plan/analyze_plan` showed an `ANNIvfPartition name=vector_idx` node (min/max nprobes 8/32 matching the server defaults, `index_comparisons=220.8K` versus 1,000,000 rows, `partitions_searched=64` across the dataset's 2 IVF_RQ segments) and a `MatchQuery` node reading `text_fts_idx` (`indices_loaded=1`), while the same query with `use_index=False` dropped the ANN node entirely (`LanceRead` full-fragment scan) and ran roughly 9x slower (33ms vs 3.8ms in-process). (4) recall@100 0.9097 < 1.0 is the expected signature of bounded-nprobes ANN. Verdict: indexes served every measured query. With that established, this run fixes the FTS leg's structural defect: `bench/reconcile.py:production_slice_batch` ingested a placeholder `f"cluster {cluster} document {i}"` string with cluster assigned by `index % num_clusters`, while `bench/search.py`'s FTS/hybrid legs built queries from `prepare.py`'s independently k-means-seeded `cluster_vocab`/`clusters.npy` — the two vocabularies shared zero tokens by construction, so `hit_rate` was structurally 0 regardless of whether the INVERTED index worked. Fix: `bench/reconcile.py` now loads the exact `centroids.npy`/`vocab.json` artifacts `bench/prepare.py` persisted (new `load_prepared_vocab`), assigns each row's cluster with the same `bench.corpus.assign_clusters` call `clusters.npy` was built from, and generates each row's document text through the same `DatasetAdapter.text_for_row` hook `prepare.py`'s Iceberg-source path already used — so the ingested corpus, `clusters.npy`, and the FTS/hybrid query vocabulary are now one shared, seeded source. The `--no-text` placeholder path (no vocabulary exists) is preserved unchanged, only now also using the real per-row cluster assignment instead of the old modulo scheme. | Reconcile drain: batch 0 — 500,000 rows, published at Lance v64, 65.225s. Batch 1 — 1,000,000 rows, published at v125, 43.307s. `publication_verification.ok == true`, all 6 indexes present, no `missing_indexes`. Zero commit retries. `final_catalog_grpc.status == "MEASURED"`: cold_ms 154.532 / warm_ms 4.196. **recall@1 0.9833, recall@10 0.9885, recall@100 0.9111** (vs the prior run's 0.9839/0.989/0.9097 — within normal ANN run-to-run noise, vector generation and ground truth are untouched by this fix), mean query 12.957ms over 10,000 queries. **FTS: hit_rate 1.0** (100 queries) — expected and provably correct, not a ceiling artifact: `build_vocabulary` makes every cluster's 40 words pairwise-distinct across all 64 clusters and the common pool, and the FTS query samples 3 terms exclusively from the queried cluster's private vocabulary, so any document containing one of those terms can only belong to that cluster by construction (spot-checked: a real corpus term retrieved via `full_text_search` returned 10/10 hits all tagged `cluster: '35'`, the queried cluster). Re-ran the plan evidence on this new v125 publication: `explain_plan`/`analyze_plan` for the same real term again shows the `MatchQuery` node over `text_fts_idx` (`indices_loaded=1`, no flat scan), and the fresh telemetry capture again shows `index/memory/hit` 943,665 times against 10,002/100/99 RPCs, confirming the INVERTED index served FTS in this run too, not merely that `hit_rate` is nonzero. **Hybrid (RRF): recall@10 0.494** (down from the prior run's 0.834) — this drop is the expected, correct signature of a real text contribution: previously the text leg returned non-matching garbage and RRF collapsed to near-pure-vector ranking (0.834 tracked vector recall@10 0.989 almost exactly). Now the text leg returns real same-cluster documents that compete with the vector-only ground truth in the RRF blend, displacing some true nearest neighbors, so recall@10 falls toward roughly half. That is a fused ranking with a genuine, cluster-consistent text signal, not a regression. `search_api.rpc.duration_ms`: vector_search mean 11.75ms/10,002 calls, hybrid_search mean 5.06ms/99 calls, text_search mean 3.77ms/100 calls (up from 0.43ms pre-fix, consistent with genuine index term lookups replacing instant negative lookups against never-matching terms). pytest: 780 passed (`-m "not integration"`), ruff clean. No stray `search-api` process or `lance_bench_e2e_*` schema left after the run. | The prior row's dataset (v132) was deleted before this run to force a clean re-ingest (`bench prepare --force` regenerated byte-identical artifacts since `prepare.py` itself needed no change — only `reconcile.py`'s ingest text generation was the defect), so its four Phase-A evidence lines above are recorded here verbatim since they are no longer independently re-derivable from disk. `bench/README.md` and the `e2e-bench` skill needed no changes: neither documented the placeholder-text limitation (only `LEDGER.md` did, in the row below), and the FTS leg's own description of "deterministic cluster-vocabulary text queries" in `bench/search.py`'s module docstring was already accurate about the query side. |
| 2026-07-21 | 20260721T140000Z (e2e) + 20260721T140000Z-search (standalone) | sift1m 1M, WITH text | 2 | **Search-leg architecture fix**: `e2e` now self-hosts a `search-api` subprocess inside the PostgreSQL isolation window instead of dialing an external `--endpoint`, closing the two defects the prior row diagnosed (ephemeral schema invisible to an external server, and the search leg being called after the schema was already dropped). First run in this ledger with real recall/FTS/hybrid/load numbers. | Reconcile drain: batch 0 — 500,000 rows, published at Lance v67, 46.135s. Batch 1 — 1,000,000 rows, published at v132, 27.156s. `publication_verification.ok == true`, all 6 declared indexes present (`cluster_idx`, `is_deleted_bitmap_idx`, `text_fts_idx`, `ts_idx`, `ts_zonemap_idx`, `vector_idx`), no `missing_indexes`. Zero commit retries, zero pipeline failures, zero replan/spill warnings in `search-api.log`. `final_catalog_grpc.status == "MEASURED"` (self-hosted inside the isolation window, `expected_versions` resolved directly from the live repository, no evidence file needed): cold_ms 154.782 / warm_ms 4.139. **recall@1 0.9839, recall@10 0.989, recall@100 0.9097**, mean query 13.363ms over 10,000 queries. FTS: 100 queries, hit_rate 0.0, mean 1.4ms. Hybrid (RRF): recall@10 0.834, mean 5.108ms over 100 queries. Standalone `search --control-plane-url` against the same kept schema (`e2e --keep-control-plane`) reproduced the same recall/FTS/hybrid numbers and additionally measured the load leg: concurrency 1 → 235.9 qps, mean 4.235ms. Concurrency 8 → 714.6 qps, mean 11.188ms. Concurrency 32 → `FAILED` (`RESOURCE_EXHAUSTED: tenant search capacity exhausted` — expected: at the default `--tenants 1`, 32 workers all hit one org against the server's fixed `DEFAULT_PER_TENANT_SEARCH_CONCURRENCY = 8` admission cap in `rust/search-api/src/config.rs`). Overall `load.status` still reads `MEASURED` since two of three levels succeeded, and the load-leg resilience change — catching `grpc.RpcError` per concurrency level instead of crashing the whole `search` command — is what let the artifact save at all. `search_api.rpc.duration_ms`: vector_search mean 12.15ms/10,002 calls (max 152ms), hybrid_search mean 3.88ms/99 calls, text_search mean 0.43ms/100 calls. pytest: 780 passed (`-m "not integration"`) + 9 passed on the targeted integration/reconcile files, ruff clean. | **FTS hit_rate 0.0 is a real, named limitation, not a bug in this fix**: `bench/reconcile.py:production_slice_batch` ingests placeholder text (`f"cluster {cluster} document {i}"`, its own docstring calling this "a deterministic document" the benchmark "never depends on"), while `bench/search.py`'s FTS leg queries a *different*, independently-seeded `cluster_vocab` from `prepare.py` — the two vocabularies share zero tokens by construction, so every FTS query returns non-matching hits and hybrid RRF collapses to essentially vector-only ranking (hybrid recall@10 0.834 tracks vector recall@10 0.989, not a real fused signal). This is a corpus-generation mismatch spanning `reconcile.py`/`prepare.py`, orthogonal to the search-leg plumbing fixed here, and intentionally out of scope for this change — vector recall@1/10/100 is the real, unaffected headline. Design: `--endpoint` was removed from `e2e` entirely (an external server can never see the ephemeral schema). `--search-api-binary` (default `rust/search-api/target/release/search-api`, empty string disables explicitly) controls self-hosting. `--keep-control-plane` skips the schema drop and writes `control_plane.json` so a follow-up `search --control-plane-url` can self-host again for ad hoc load/FTS/hybrid work. `tokio_postgres::Config`'s own URL parser was verified (against the real binary, via a raw gRPC `NOT_FOUND` vs `UNAVAILABLE` probe) to accept the same percent-encoded `options=-csearch_path=<schema>` parameter SQLAlchemy already used for the Python side — no Rust changes were needed. New `bench/search_server.py` hand-encodes the standard gRPC health-check wire format (`grpc.health.v1.Health/Check`) rather than adding a `grpcio-health-checking` dependency. Two lingering ephemeral schemas from earlier interactive smoke testing were found and dropped (`lance_bench_e2e_*` in `lance_etl_bench`, unrelated pre-existing cruft, not created by this run) — the bench DB now has zero stray `lance_bench_e2e_*` schemas. |
| 2026-07-21 | 20260721T115105Z | sift1m 1M, WITH text (first text-enabled run) | 2 | Post TLS/auth removal, attempted full query-side qualification (recall, FTS, hybrid, load) against the plaintext, unauthenticated `search-api` release binary | Reconcile drain: batch 0 — 500,000 rows, published at Lance v67, 63.808s. Batch 1 — 1,000,000 rows, published at v132, 37.578s. `publication_verification.ok == true`, all 6 declared indexes present including `text_fts_idx` and `ts_zonemap_idx` (first time this ledger has a text-enabled build), no `missing_indexes`. Zero commit retries (`retried: 0` both batches), zero `lance.pipeline.run.datasets_failed` across 14 samples. `lance.pipeline.dataset.replay_merge_ms` summed 49.4s over 112 samples, `lance.pipeline.run.build_ms` summed 5.4s over 12 samples. `bench report` produced only `NOT_RUN` (no `search` phase artifact exists), `results.csv` empty. | **The query-side legs (recall@1/10/100, FTS, hybrid, load percentiles) could NOT be measured — this was not a TLS/auth problem.** Removing TLS/auth did not unblock them: `python -m bench e2e --endpoint 127.0.0.1:8080 ...` and standalone `python -m bench search --endpoint 127.0.0.1:8080 ...` both fail 100% of the time with gRPC `UNAVAILABLE: serving catalog unavailable`, reproduced at small scale (2,000 rows) before committing to the full run. Root cause, confirmed with `psql ... -c "\dt public.*"` returning zero relations: `bench e2e`/`bench fuzz` always create, migrate, and use a randomly-named, ephemeral PostgreSQL schema (`lance_bench_e2e_<uuid>` via `isolated_control_plane` in `bench/reconcile.py`) and drop it with CASCADE when the run's `with` block exits. There is no flag or env var to pin that schema name, so an externally-started `search-api` server (which connects with the default `search_path`, finding an empty `public` schema) can never see the published catalog rows. A second, independent defect compounds this for `e2e` specifically: `catalog_search_leg(config, grpc_gen_dir)` in `bench/e2e.py` is called *after* the `with isolated_control_plane(...)` block has already exited and dropped the schema, so the leg cannot succeed under any server configuration, even a hypothetical one pointed at the right schema name. Per the prior ledger row, the endpoint leg had never been exercised before now (recorded `NOT_RUN`), so this is a previously-undiscovered architectural gap, not a regression from the TLS/auth removal. The version-evidence file (`--search-expected-versions-path`) was never the blocker — catalog resolution fails before `validate_served_version` is reached. No product code was changed to work around this per the task's hard rule. The only route to real recall/FTS/hybrid/load numbers is publishing into a persistent, pre-migrated schema outside the isolation wrapper (new orchestration code, not yet built — needs explicit sign-off). Search-api release binary confirmed plaintext/unauthenticated (`"search-api listening (plaintext, unauthenticated)"`, ports `127.0.0.1:8080` search / `127.0.0.1:8081` health), so the CLIENT side of the TLS/auth removal is verified working — it is the ephemeral-schema design that blocks measurement, independent of TLS/auth. pylance 8.0.0 pinned. No `search-api.log` or `traces.jsonl` content for this run since no `--endpoint` was passed (expected — `bench/TELEMETRY.md` documents zero `search_api.*`/OTLP events without an RPC). |
| 2026-07-21 | 20260721T085100Z | sift1m 1M | 2 | Documentation qualification (post grand-simplification, reconciler-driven control plane) | Reconcile drain: batch 0 — 500,000 rows, published at Lance v63, 64.5s. Batch 1 — 1,000,000 rows, published at v124, 38.5s. `publication_verification.ok == true`, all 6 declared indexes present (`cluster_idx`, `is_deleted_bitmap_idx`, `text_fts_idx`, `ts_idx`, `ts_zonemap_idx`, `vector_idx`), no `missing_indexes`. Zero commit retries. Zero pipeline failures. | `final_catalog_grpc.status == "NOT_RUN"` — search leg is credential-gated and no `--search-ca-path`/`--search-token-dir`/`--search-expected-versions-path` were configured for this run. pylance 8.0.0 pinned (per `pyproject.toml`). `cargo build --release` for `rust/search-api` took roughly 20 minutes. |

## Historical baseline (pre-dates this ledger)

The e2e-bench skill's baseline anchor, run `20260611T203117Z` (2026-06-11) documented in
`SKILL.md`, predates both this ledger and the current `e2e.json` schema
(`batches[]` / `publications[]` / `publication_verification` / `final_catalog_grpc`). It measured
a vector-only build against the retired batch-major pipeline path and is not comparable to the
rows above.
