apiVersion: v1
kind: Service
metadata:
  name: lance-etl-search-internal
  labels:
    app.kubernetes.io/name: lance-etl-search
    app.kubernetes.io/component: search
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    app.kubernetes.io/name: lance-etl-search
    app.kubernetes.io/track: stable
  ports:
    - name: grpc
      port: 8080
      targetPort: grpc
      protocol: TCP
