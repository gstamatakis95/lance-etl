[project]
name = "lance-etl"
version = "0.2.0"
requires-python = ">=3.12"
dependencies = [
    "pylance==8.0.0",
    "pyarrow>=23",
    "pyspark==4.0.1",
    "psutil>=7,<8",
    "pandas>=2.2.0",
    "numpy",
    "datadog",
    "ddtrace",
    "psycopg[binary]>=3.2,<4",
    "sqlalchemy>=2.0,<3",
    "alembic>=1.16,<2",
]

[project.scripts]
lance-etl-reconcile = "lance_etl.reconciler.launcher:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/lance_etl"]

[dependency-groups]
dev = ["pytest", "ruff"]
bench = ["matplotlib", "grpcio", "grpcio-tools", "opentelemetry-proto"]

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = ["bench/workspace*", "bench/corpora/"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "UP", "SIM", "ARG", "PLC0415", "D", "ANN", "C901"]
ignore = ["ANN401"]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["C901"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.ruff.lint.mccabe]
max-complexity = 12

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: slow end-to-end tests that run Spark in local mode",
]
